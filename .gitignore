# Editor
.idea/
.vscode/
*.swp
*.swo

# macOS
.DS_Store

# Generated bindings consumed by external code (consumers vendor
# them per-language). The Rust source under src/ is THIS repo's
# canonical crate, not generated bindings, so it's tracked.
generated/

# Rust build artifacts
target/
Cargo.lock
